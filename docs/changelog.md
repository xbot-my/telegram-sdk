# 更新日志

此页面汇总了 XBot Telegram SDK 的主要版本变更。日期均使用 **Asia/Kuala_Lumpur (UTC+8)** 时区。

---

## v1.0.0 — 2025‑08‑28

* 🎉 发布首个稳定版本，覆盖 Telegram Bot API 9.2 的全部方法。
* 支持多 Bot、多实例管理与自动实例隔离。
* 提供基于 Guzzle 的高性能 HTTP 客户端，并内置重试与异常处理机制。
* 新增 Laravel 集成：ServiceProvider、配置文件、Webhook 路由和更新处理器。
* 支持链式调用和统一响应转换 (`toArray()` / `toObject()` / `toJson()`)。
* 发布完整使用文档及 API 覆盖映射。

## v0.9.x — 2025‑08‑15

* 🔄 引入 Bot API 9.2 支持，包括 `direct_messages_topic_id`、`suggested_post_parameters` 和 `reply_parameters.checklist_task_id` 等新参数【736136284671642†L188-L215】。
* 添加 `GetChatBoosts`、`GetUserChatBoosts`、`RefundStarPayment`、`ReadBusinessMessage` 等业务相关端点【736136284671642†L165-L183】。
* 增强日志系统，可通过环境变量控制日志级别和输出通道【736136284671642†L222-L233】。
* 提升异常信息的可读性，记录重试原因和延迟时间。

> 如需查看完整历史记录和每个提交的差异，请访问 [GitHub Releases](https://github.com/xbot-my/telegram-sdk/releases)。
